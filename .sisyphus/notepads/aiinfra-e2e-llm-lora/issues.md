# Issues


- Task 1 issue: persistent shell session did not resolve newly installed console scripts (`aiinfra-e2e`, `ruff`) on PATH, so verification used the active Python bin path directly; LSP diagnostics also could not discover basedpyright-langserver despite installation.
- Task 3 issue: changed-file LSP diagnostics still report import-stub warnings for local project modules and a Typer context `obj` Any warning, even though runtime behavior, Ruff, and pytest all pass.
- Task 4 issue: basedpyright briefly failed to resolve newly added `aiinfra_e2e.data` package imports in tests and package re-exports even while `pytest` succeeded; importing the concrete submodules inside a small test helper avoided the false-positive error and kept changed-file diagnostics clean.
- Task 7 issue: changed-file LSP diagnostics still report the repo's existing missing-stub and partially-unknown third-party typing warnings (datasets/TRL/PEFT/MLflow), but there were no new diagnostics errors and both `ruff check .` and `pytest -q` passed after the checkpoint/resume changes.
- Task 10 issue: changed-file LSP diagnostics could not fully validate this config-only work in this environment because Markdown has no configured LSP, Makefile has no configured LSP, and `yaml-language-server` is configured but not installed, so validation used `docker compose ... config` plus JSON/YAML syntax parsing instead.
- Task 11 issue: basedpyright in this environment could not resolve optional `locust` imports, so the standalone Locust script uses dynamic imports plus a file-local pyright suppression to stay CPU-testable and diagnostics-clean without adding new dependencies.

- 2026-04-13 Final Verification Wave F2 code review: REJECT pending fixes for (1) ignored DataConfig field overrides in `src/aiinfra_e2e/train/sft.py`, (2) private `trl.trainer.*` imports in `src/aiinfra_e2e/train/sft.py` and tests, (3) runtime behavior keyed off `PYTEST_CURRENT_TEST`, (4) CI smoke target is still placeholder while README points to `scripts/smoke_cpu.sh`, and (5) `ManagedVLLMServer` pipes stdout/stderr without consuming them.

- 2026-04-13 Final Verification Wave F4 scope fidelity: REJECT. Verified DoD command exits were green for `docker compose -f docker/compose.yaml config`, `python -m pip install -e .`, `ruff check .`, and `pytest -q`, but plan-required Makefile orchestration diverges: `make smoke`, `make e2e`, `make serve`, and `make loadtest` are placeholders rather than executing the documented pipeline/scripts; additionally, plan file-map requires `src/aiinfra_e2e/obs/mlflow.py` which is absent.

- 2026-04-13 ManagedVLLMServer subprocess IO review: `src/aiinfra_e2e/serve/vllm_server.py` launches vLLM via `subprocess.Popen(..., stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)` in `ManagedVLLMServer.start` (lines 125-131) but has no background drain/consumer for either stream while the process runs. Only `stderr.read()` is called in `wait_until_ready` after `poll()` indicates process exit (lines 138-142). This can deadlock startup: if vLLM writes enough logs to fill OS pipe buffer before readiness, child blocks on write, server never reaches ready state, and wrapper eventually times out.
- Minimal no-dependency fixes: (A) safest/smallest: remove PIPEs and inherit parent streams (`stdout=None, stderr=None`), (B) redirect both to append log file handle(s) opened by wrapper, or (C) keep PIPEs but start 2 daemon drain threads (stdout/stderr) that continuously read lines until EOF.

- 2026-04-13 Final Verification Wave F2 evidence refresh (training/config):
  - Runtime keyed to test env var still present: `src/aiinfra_e2e/train/sft.py:81-88` (`_is_cpu_smoke_mode`) and `:97-99` (`_should_enable_qlora`) branch on `PYTEST_CURRENT_TEST`; smoke script sets it in `scripts/smoke_cpu.sh:95`.
  - Private TRL imports still present in runtime code: `src/aiinfra_e2e/train/sft.py:27-28` imports `trl.trainer.sft_config` / `trl.trainer.sft_trainer`; test also imports private path at `tests/test_resume_checkpoint.py:95`.
  - DataConfig field overrides remain ignored in SFT path: `DataConfig` defines `text_field/prompt_field/response_field` in `src/aiinfra_e2e/config.py:24-26`, but no `data_config.<field>` usage in code path; hardcoded schema appears at `src/aiinfra_e2e/data/preprocess.py:46-49` (instruction/input/output defaults), `src/aiinfra_e2e/train/sft.py:221` (preprocess called without field overrides), `:224` (select_columns hardcodes `"text"`), and `:284` (`dataset_text_field="text"`).

- 2026-04-13T23:17:19 Final Verification Wave F2 training/config fixes: replaced production/runtime branching on `PYTEST_CURRENT_TEST` with explicit `AIINFRA_E2E_CPU_SMOKE=1` in `src/aiinfra_e2e/train/sft.py` and `scripts/smoke_cpu.sh`; routed TRL SFT imports through new `src/aiinfra_e2e/train/trl_compat.py` so runtime code no longer imports `trl.trainer.*` directly; threaded `DataConfig.text_field`, `prompt_field`, and `response_field` through `src/aiinfra_e2e/train/sft.py` and `src/aiinfra_e2e/data/preprocess.py` while preserving existing defaults; updated focused tests so CPU smoke behavior is explicit and new field/compat contracts are locked by regression coverage.

- 2026-04-13T15:28:20+00:00 ManagedVLLMServer PIPE deadlock fix: added a regression test in tests/test_openai_api_smoke.py that captures subprocess.Popen kwargs and proves start() does not launch vLLM with undrained stdout/stderr PIPEs; updated src/aiinfra_e2e/serve/vllm_server.py to inherit parent stdio by removing both PIPE args, which avoids startup hangs while preserving early-exit stderr text when a stream is available.
- 2026-04-13T23:39:57+08:00 Final Verification Wave F2 TRL compat follow-up: removed the last private TRL fallback from `src/aiinfra_e2e/train/trl_compat.py` so `load_trl_sft_objects()` now accepts only public `trl.SFTTrainer`/`trl.SFTConfig` exports and raises a clear `RuntimeError` otherwise; added a regression test in `tests/test_resume_checkpoint.py` that fails if any trainer-internal TRL import path is attempted.
- 2026-04-13T23:53:27+08:00 Plan-compliance smoke output fix: updated scripts/smoke_cpu.sh to keep temporary inline config generation under mktemp, but default durable outputs now write to repo-owned paths (`artifacts/runs`, `mlruns`, and `artifacts/env`) unless `CPU_SMOKE_RUN_ROOT`, `CPU_SMOKE_TRACKING_URI`, or `CPU_SMOKE_ENV_OUT` overrides are provided; added a focused regression assertion in tests/test_train_smoke_cpu.py and re-verified with ruff, pytest, and make smoke.
- 2026-04-14T00:00:00+00:00 GPU e2e HF cache fix: `make e2e` was failing in `scripts/e2e_gpu.sh` during `==> Data sync` because `HF_HOME=/mnt/vtg_dataset/LLM` caused Hugging Face to write dataset cache files under `/mnt/vtg_dataset/LLM/hub/datasets--hfl--alpaca_zh_51k`, which was not writable. The script now sets repo-local defaults for `HF_HOME`, `HF_HUB_CACHE`, `HF_DATASETS_CACHE`, and `TRANSFORMERS_CACHE` only when they are not already provided, creates those directories up front, and prints the active cache paths for future audits.
- 2026-04-14T01:05:39+08:00 GPU serve readiness proxy bypass fix: `make e2e`/serve readiness polling was failing with `HTTP Error 403: Forbidden` (or proxy `URLError`) because `openai_request()` used `urllib.request.urlopen()`, which honors `http_proxy`/`HTTP_PROXY` in this environment (`http_proxy=http://child-prc.intel.com:913`). `no_proxy` used CIDR entries that urllib does not honor for localhost polling, so `http://127.0.0.1:<port>/v1/models` was sent through the corporate proxy instead of the local server. Repro before fix: set `http_proxy`/`HTTP_PROXY` to an invalid proxy, start a tiny local HTTP server on `/v1/models`, and call `openai_request("http://127.0.0.1:<port>", "/v1/models")` -> connection/proxy failure. Fix: detect localhost-ish base URLs (`localhost`, `127.0.0.1`, `0.0.0.0`) and use `urllib.request.build_opener(ProxyHandler({}))` for those requests only.
- 2026-04-14T12:01:02+08:00 GPU e2e serve port conflict workaround: on this host, binding the configured serve port 8000 fails because another long-running local service already owns it, and requests to `http://127.0.0.1:8000/v1/models` hit that foreign service instead of the repo's wrapper readiness target. `scripts/e2e_gpu.sh` now preserves the checked-in/user-provided source YAML paths, but writes effective temp serve/loadtest configs under `$TMP_DIR` by loading them through `aiinfra_e2e.config.load_yaml`, selecting free localhost serve/metrics ports only when the configured ports are occupied, and rewriting the temp loadtest target to the chosen serve port so readiness and loadtest stay aligned without changing checked-in defaults.
- 2026-04-14T13:17:50+08:00 vLLM Qwen tokenizer compat fix: this environment uses `transformers==5.0.0` and `vllm==0.10.2rc2.dev24+g8d58c6d74.d20260228.cu129`. Reproduced root cause with both `AutoTokenizer.from_pretrained('Qwen/Qwen2.5-7B-Instruct')` (using a writable local HF cache override) and a cached local Qwen-family snapshot (`/mnt/vtg_dataset/LLM/hub/models--deepseek-ai--DeepSeek-R1-Distill-Qwen-1.5B/...`): tokenizer objects (`Qwen2Tokenizer` / `TokenizersBackend`) expose `all_special_tokens` but raise `AttributeError` for `all_special_tokens_extended`, while local vLLM source reads `tokenizer.all_special_tokens_extended` in `vllm/transformers_utils/tokenizer.py`. Fix: add `src/aiinfra_e2e/serve/tokenizer_compat.py` with `ensure_all_special_tokens_extended()` that patches `transformers.PreTrainedTokenizerBase` with a compatibility property returning `list(self.all_special_tokens)` when the attribute is missing; add repo-root `sitecustomize.py` guarded by `AIINFRA_E2E_ENABLE_VLLM_TOKENIZER_COMPAT=1`; and update `src/aiinfra_e2e/serve/vllm_server.py:build_vllm_environment()` to prepend the repo root to child `PYTHONPATH` and set that env flag only for the spawned vLLM subprocess. Regression coverage in `tests/test_openai_api_smoke.py` proves a real local `Qwen2Tokenizer` gains `.all_special_tokens_extended == list(all_special_tokens)`. Live serve evidence: with `CUDA_VISIBLE_DEVICES=2`, wrapper-built env (`compat_flag=1`, repo-root `PYTHONPATH`) and local cached Qwen-family model path, `python -m vllm.entrypoints.openai.api_server ...` reached readiness and `/v1/models` returned the served model JSON instead of crashing during tokenizer setup.
- 2026-04-14T13:19:41+08:00 vLLM tqdm duplicate-disable compat fix: reproduced the serve-stage crash from local vLLM source rather than guessing. Root cause is `vllm.model_executor.model_loader.weight_utils.DisabledTqdm.__init__`, which currently calls `super().__init__(*args, **kwargs, disable=True)`; in this environment the base class is `tqdm.asyncio.tqdm_asyncio`, so any caller that already passes `disable=...` raises `TypeError: tqdm.asyncio.tqdm_asyncio.__init__() got multiple values for keyword argument 'disable'` before the tqdm constructor body runs. Fix: add `src/aiinfra_e2e/serve/tqdm_compat.py` with `ensure_vllm_tqdm_compat()` that dynamically imports vLLM and monkeypatches `DisabledTqdm.__init__` to drop any caller-provided `disable` kwarg before delegating to the original method; extend repo-root `sitecustomize.py` with a second guard `AIINFRA_E2E_ENABLE_VLLM_TQDM_COMPAT=1`; and update `src/aiinfra_e2e/serve/vllm_server.py:build_vllm_environment()` to set that flag only for the spawned vLLM subprocess. Regression coverage in `tests/test_openai_api_smoke.py` reloads `weight_utils`, proves raw `DisabledTqdm(total=1, disable=False)` raises the duplicate-`disable` `TypeError`, then applies `ensure_vllm_tqdm_compat()` and proves the same call succeeds with `progress.disable is True`. Verification: `pytest -q` passed (`49 passed, 1 skipped`), and an isolated wrapper-style startup using cached model `/mnt/vtg_dataset/LLM/hub/models--deepseek-ai--DeepSeek-R1-Distill-Qwen-1.5B/snapshots/ad9f0ae0864d7fbcd1cd905e3c6c5b069cc8b562`, wrapper-built env (`AIINFRA_E2E_ENABLE_VLLM_TQDM_COMPAT=1`, tokenizer compat still `1`, repo-root `PYTHONPATH`), `CUDA_VISIBLE_DEVICES=2`, and extra args `--enforce-eager --max-model-len 2048` reached `Starting vLLM API server ...` and returned `200 OK` from `/v1/models` with `id: smoke-model`.
- 2026-04-14T13:30:00+08:00 Serve readiness host normalization fix: `0.0.0.0` is correct for binding the server socket, but it is not connectable from client-side readiness polling or Locust. `ServeConfig.base_url` now normalizes wildcard bind hosts (`0.0.0.0`, `::`, and empty host strings) to `127.0.0.1` while leaving ordinary explicit hosts unchanged, so `make e2e` readiness and loadtest calls resolve to loopback instead of `http://0.0.0.0:<port>/v1/models`.
- 2026-04-14T14:05:00+08:00 GPU e2e GPU-selection fix: `scripts/e2e_gpu.sh` was still leaving `CUDA_VISIBLE_DEVICES` unset, so the training and serve stages defaulted to GPU0 on this shared host even when GPU0/1 were occupied by external `VLLM::Worker_*` processes and GPU2/3 had ~81 GiB free. Added `src/aiinfra_e2e/gpu.py:select_cuda_visible_devices()` with a minimal `subprocess.run(["nvidia-smi", "--query-gpu=index,memory.free", "--format=csv,noheader,nounits"])` parser that preserves an existing `CUDA_VISIBLE_DEVICES` value and otherwise picks the single GPU with the most free VRAM (lowest index on ties); `scripts/e2e_gpu.sh` now calls it once near the top and logs the chosen value so both Train and Serve inherit the same binding. Regression coverage: `pytest -q tests/test_gpu_selection.py` passed after first failing on the missing helper. Host verification: with `CUDA_VISIBLE_DEVICES` unset, the script selection block logged `CUDA_VISIBLE_DEVICES=2`, and `make e2e` now gets past GPU selection instead of immediately aiming at busy GPU0/1; the current remaining blocker on this host is unrelated import-time failure in `transformers`/`peft` due to broken `torchvision` (`RuntimeError: operator torchvision::nms does not exist`, followed by `ModuleNotFoundError: Could not import module 'PreTrainedModel'`) during `==> Validating configs`.
- 2026-04-14T14:45:00+08:00 Pytest collection import-coupling fix: `pytest -q` collection was failing because importing `aiinfra_e2e.cli` eagerly imported `aiinfra_e2e.train.sft` (via both `src/aiinfra_e2e/cli.py` and `src/aiinfra_e2e/train/__init__.py`), which pulled `peft -> transformers -> torchvision` and crashed on this host's broken torchvision install before tests ran. Fix: move the `run_sft_from_paths` import inside `train_sft_command`, replace eager `train.__init__` re-exports with lazy `__getattr__`, and make `src/aiinfra_e2e/train/sft.py`/`src/aiinfra_e2e/train/trl_compat.py` runtime-load heavy TRL/PEFT pieces instead of importing them at module load time. Added regression coverage in `tests/test_cli_import_lazy.py` proving `import aiinfra_e2e.cli` does not import `peft` or `torchvision`; then hardened related training/serve tests so they stub their own trainer/vLLM globals and avoid cross-test module/registry pollution. Verification: `python -m aiinfra_e2e.cli --help` works and `pytest -q` passes (`54 passed, 1 skipped`).
- 2026-04-14T16:51:00+08:00 Train-stage torchvision compat fix: `make e2e` originally failed in `==> Train` because `trl.SFTTrainer` imports `transformers.AutoProcessor`, which reaches `transformers.image_utils`; in transformers 5.0.0 `is_torchvision_available()` only checks package presence, so an installed-but-broken `torchvision` still imports and crashes (`AttributeError: partially initialized module 'torchvision' has no attribute 'extension'` / `operator torchvision::nms does not exist`). Added `disable_torchvision_if_broken()` in `src/aiinfra_e2e/train/trl_compat.py` to probe `import torchvision`, clear partial `torchvision*` modules, patch both `transformers.utils.is_torchvision_available` and `transformers.utils.import_utils.is_torchvision_available` to return `False`, clear cached wrappers when present, and log a warning before importing TRL. Also fixed two host-only Train follow-ons exposed by getting past that import: `_load_model` now uses stringified `cast("PreTrainedModel", ...)` so TYPE_CHECKING-only symbols do not raise `NameError` at runtime, and CPU smoke fallback now uses safetensors-backed `hf-internal-testing/tiny-random-gpt2` with a 512-token clamp applied consistently to trainer args and pretokenized examples so this host can complete Train without exceeding the tiny model's real position limit. Verification: changed-file LSP diagnostics reported no errors, `pytest -q` passed (`59 passed, 1 skipped`), and `make e2e` progressed through `==> Train` and `==> Offline eval`; the remaining host blocker is later in `==> Serve`, where vLLM still hits the broken torchvision / `ProcessorMixin` import path outside the train-only compat shim.
